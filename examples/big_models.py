"""示例：大型自动生成的数据模型集合（用于演示分层上下文 L4 大 PR 路径）。"""

from dataclasses import dataclass

@dataclass
class Entity1:
    """实体 1。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity2:
    """实体 2。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity3:
    """实体 3。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity4:
    """实体 4。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity5:
    """实体 5。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity6:
    """实体 6。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity7:
    """实体 7。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity8:
    """实体 8。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity9:
    """实体 9。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity10:
    """实体 10。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity11:
    """实体 11。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity12:
    """实体 12。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity13:
    """实体 13。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity14:
    """实体 14。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity15:
    """实体 15。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity16:
    """实体 16。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity17:
    """实体 17。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity18:
    """实体 18。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity19:
    """实体 19。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity20:
    """实体 20。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity21:
    """实体 21。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity22:
    """实体 22。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity23:
    """实体 23。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity24:
    """实体 24。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity25:
    """实体 25。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity26:
    """实体 26。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity27:
    """实体 27。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity28:
    """实体 28。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity29:
    """实体 29。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity30:
    """实体 30。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity31:
    """实体 31。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity32:
    """实体 32。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity33:
    """实体 33。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity34:
    """实体 34。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity35:
    """实体 35。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity36:
    """实体 36。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity37:
    """实体 37。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity38:
    """实体 38。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity39:
    """实体 39。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity40:
    """实体 40。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity41:
    """实体 41。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity42:
    """实体 42。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity43:
    """实体 43。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity44:
    """实体 44。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity45:
    """实体 45。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity46:
    """实体 46。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity47:
    """实体 47。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity48:
    """实体 48。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity49:
    """实体 49。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity50:
    """实体 50。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity51:
    """实体 51。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity52:
    """实体 52。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity53:
    """实体 53。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity54:
    """实体 54。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity55:
    """实体 55。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity56:
    """实体 56。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity57:
    """实体 57。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity58:
    """实体 58。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity59:
    """实体 59。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity60:
    """实体 60。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity61:
    """实体 61。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity62:
    """实体 62。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity63:
    """实体 63。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity64:
    """实体 64。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity65:
    """实体 65。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity66:
    """实体 66。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity67:
    """实体 67。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity68:
    """实体 68。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity69:
    """实体 69。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity70:
    """实体 70。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity71:
    """实体 71。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity72:
    """实体 72。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity73:
    """实体 73。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity74:
    """实体 74。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity75:
    """实体 75。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity76:
    """实体 76。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity77:
    """实体 77。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity78:
    """实体 78。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity79:
    """实体 79。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity80:
    """实体 80。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity81:
    """实体 81。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity82:
    """实体 82。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity83:
    """实体 83。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity84:
    """实体 84。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity85:
    """实体 85。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity86:
    """实体 86。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity87:
    """实体 87。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity88:
    """实体 88。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity89:
    """实体 89。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity90:
    """实体 90。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity91:
    """实体 91。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity92:
    """实体 92。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity93:
    """实体 93。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity94:
    """实体 94。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity95:
    """实体 95。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity96:
    """实体 96。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity97:
    """实体 97。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity98:
    """实体 98。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity99:
    """实体 99。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity100:
    """实体 100。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity101:
    """实体 101。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity102:
    """实体 102。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity103:
    """实体 103。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity104:
    """实体 104。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity105:
    """实体 105。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity106:
    """实体 106。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity107:
    """实体 107。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity108:
    """实体 108。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity109:
    """实体 109。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity110:
    """实体 110。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity111:
    """实体 111。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity112:
    """实体 112。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity113:
    """实体 113。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity114:
    """实体 114。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity115:
    """实体 115。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity116:
    """实体 116。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity117:
    """实体 117。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity118:
    """实体 118。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity119:
    """实体 119。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity120:
    """实体 120。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity121:
    """实体 121。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity122:
    """实体 122。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity123:
    """实体 123。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity124:
    """实体 124。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity125:
    """实体 125。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity126:
    """实体 126。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity127:
    """实体 127。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity128:
    """实体 128。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity129:
    """实体 129。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity130:
    """实体 130。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity131:
    """实体 131。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity132:
    """实体 132。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity133:
    """实体 133。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity134:
    """实体 134。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity135:
    """实体 135。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity136:
    """实体 136。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity137:
    """实体 137。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity138:
    """实体 138。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity139:
    """实体 139。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity140:
    """实体 140。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity141:
    """实体 141。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity142:
    """实体 142。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity143:
    """实体 143。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity144:
    """实体 144。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity145:
    """实体 145。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity146:
    """实体 146。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity147:
    """实体 147。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity148:
    """实体 148。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity149:
    """实体 149。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity150:
    """实体 150。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity151:
    """实体 151。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity152:
    """实体 152。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity153:
    """实体 153。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity154:
    """实体 154。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity155:
    """实体 155。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity156:
    """实体 156。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity157:
    """实体 157。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity158:
    """实体 158。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity159:
    """实体 159。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity160:
    """实体 160。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity161:
    """实体 161。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity162:
    """实体 162。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity163:
    """实体 163。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity164:
    """实体 164。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity165:
    """实体 165。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity166:
    """实体 166。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity167:
    """实体 167。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity168:
    """实体 168。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity169:
    """实体 169。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity170:
    """实体 170。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity171:
    """实体 171。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity172:
    """实体 172。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity173:
    """实体 173。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity174:
    """实体 174。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity175:
    """实体 175。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity176:
    """实体 176。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity177:
    """实体 177。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity178:
    """实体 178。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity179:
    """实体 179。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity180:
    """实体 180。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity181:
    """实体 181。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity182:
    """实体 182。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity183:
    """实体 183。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity184:
    """实体 184。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity185:
    """实体 185。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity186:
    """实体 186。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity187:
    """实体 187。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity188:
    """实体 188。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity189:
    """实体 189。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity190:
    """实体 190。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity191:
    """实体 191。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity192:
    """实体 192。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity193:
    """实体 193。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity194:
    """实体 194。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity195:
    """实体 195。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity196:
    """实体 196。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity197:
    """实体 197。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity198:
    """实体 198。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity199:
    """实体 199。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity200:
    """实体 200。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity201:
    """实体 201。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity202:
    """实体 202。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity203:
    """实体 203。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity204:
    """实体 204。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity205:
    """实体 205。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity206:
    """实体 206。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity207:
    """实体 207。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity208:
    """实体 208。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity209:
    """实体 209。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity210:
    """实体 210。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity211:
    """实体 211。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity212:
    """实体 212。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity213:
    """实体 213。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity214:
    """实体 214。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity215:
    """实体 215。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity216:
    """实体 216。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity217:
    """实体 217。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity218:
    """实体 218。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity219:
    """实体 219。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity220:
    """实体 220。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity221:
    """实体 221。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity222:
    """实体 222。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity223:
    """实体 223。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity224:
    """实体 224。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity225:
    """实体 225。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity226:
    """实体 226。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity227:
    """实体 227。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity228:
    """实体 228。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity229:
    """实体 229。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity230:
    """实体 230。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity231:
    """实体 231。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity232:
    """实体 232。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity233:
    """实体 233。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity234:
    """实体 234。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity235:
    """实体 235。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity236:
    """实体 236。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity237:
    """实体 237。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity238:
    """实体 238。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity239:
    """实体 239。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity240:
    """实体 240。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity241:
    """实体 241。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity242:
    """实体 242。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity243:
    """实体 243。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity244:
    """实体 244。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity245:
    """实体 245。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity246:
    """实体 246。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity247:
    """实体 247。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity248:
    """实体 248。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity249:
    """实体 249。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity250:
    """实体 250。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity251:
    """实体 251。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity252:
    """实体 252。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity253:
    """实体 253。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity254:
    """实体 254。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity255:
    """实体 255。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity256:
    """实体 256。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity257:
    """实体 257。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity258:
    """实体 258。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity259:
    """实体 259。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity260:
    """实体 260。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity261:
    """实体 261。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity262:
    """实体 262。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity263:
    """实体 263。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity264:
    """实体 264。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity265:
    """实体 265。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity266:
    """实体 266。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity267:
    """实体 267。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity268:
    """实体 268。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity269:
    """实体 269。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity270:
    """实体 270。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity271:
    """实体 271。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity272:
    """实体 272。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity273:
    """实体 273。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity274:
    """实体 274。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity275:
    """实体 275。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity276:
    """实体 276。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity277:
    """实体 277。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity278:
    """实体 278。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity279:
    """实体 279。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity280:
    """实体 280。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity281:
    """实体 281。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity282:
    """实体 282。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity283:
    """实体 283。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity284:
    """实体 284。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity285:
    """实体 285。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity286:
    """实体 286。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity287:
    """实体 287。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity288:
    """实体 288。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity289:
    """实体 289。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity290:
    """实体 290。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity291:
    """实体 291。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity292:
    """实体 292。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity293:
    """实体 293。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity294:
    """实体 294。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity295:
    """实体 295。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity296:
    """实体 296。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity297:
    """实体 297。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity298:
    """实体 298。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity299:
    """实体 299。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity300:
    """实体 300。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity301:
    """实体 301。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity302:
    """实体 302。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity303:
    """实体 303。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity304:
    """实体 304。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity305:
    """实体 305。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity306:
    """实体 306。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity307:
    """实体 307。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity308:
    """实体 308。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity309:
    """实体 309。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity310:
    """实体 310。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity311:
    """实体 311。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity312:
    """实体 312。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity313:
    """实体 313。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity314:
    """实体 314。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity315:
    """实体 315。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity316:
    """实体 316。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity317:
    """实体 317。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity318:
    """实体 318。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity319:
    """实体 319。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity320:
    """实体 320。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity321:
    """实体 321。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity322:
    """实体 322。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity323:
    """实体 323。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity324:
    """实体 324。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity325:
    """实体 325。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity326:
    """实体 326。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity327:
    """实体 327。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity328:
    """实体 328。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity329:
    """实体 329。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity330:
    """实体 330。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity331:
    """实体 331。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity332:
    """实体 332。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity333:
    """实体 333。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity334:
    """实体 334。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity335:
    """实体 335。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity336:
    """实体 336。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity337:
    """实体 337。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity338:
    """实体 338。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity339:
    """实体 339。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity340:
    """实体 340。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity341:
    """实体 341。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity342:
    """实体 342。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity343:
    """实体 343。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity344:
    """实体 344。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity345:
    """实体 345。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity346:
    """实体 346。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity347:
    """实体 347。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity348:
    """实体 348。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity349:
    """实体 349。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity350:
    """实体 350。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity351:
    """实体 351。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity352:
    """实体 352。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity353:
    """实体 353。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity354:
    """实体 354。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity355:
    """实体 355。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity356:
    """实体 356。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity357:
    """实体 357。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity358:
    """实体 358。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity359:
    """实体 359。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity360:
    """实体 360。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity361:
    """实体 361。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity362:
    """实体 362。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity363:
    """实体 363。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity364:
    """实体 364。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity365:
    """实体 365。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity366:
    """实体 366。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity367:
    """实体 367。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity368:
    """实体 368。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity369:
    """实体 369。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity370:
    """实体 370。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity371:
    """实体 371。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity372:
    """实体 372。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity373:
    """实体 373。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity374:
    """实体 374。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity375:
    """实体 375。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity376:
    """实体 376。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity377:
    """实体 377。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity378:
    """实体 378。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity379:
    """实体 379。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity380:
    """实体 380。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity381:
    """实体 381。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity382:
    """实体 382。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity383:
    """实体 383。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity384:
    """实体 384。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity385:
    """实体 385。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity386:
    """实体 386。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity387:
    """实体 387。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity388:
    """实体 388。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity389:
    """实体 389。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity390:
    """实体 390。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity391:
    """实体 391。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity392:
    """实体 392。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity393:
    """实体 393。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity394:
    """实体 394。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity395:
    """实体 395。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity396:
    """实体 396。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity397:
    """实体 397。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity398:
    """实体 398。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity399:
    """实体 399。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

@dataclass
class Entity400:
    """实体 400。"""
    id: int
    name: str
    value: float = 0.0

    def total(self, qty: int) -> float:
        # 注意：金额用 float 仅为示例，生产应用 Decimal
        return self.value * qty

