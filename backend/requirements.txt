# ===== 后端运行依赖（PR1 只引入跑通骨架所需，其余 PR 再按需追加）=====
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
# LLM 与 GitHub 客户端（PR3/PR4 开始真正使用，先在骨架阶段锁版本）
openai==1.59.6
PyGithub==2.5.0
httpx==0.28.1
# 用户系统（PR46：SQLite ORM + bcrypt 密码哈希 + JWT 鉴权）
sqlalchemy==2.0.36
bcrypt==4.2.1
PyJWT==2.10.1
# PAT 加密存储（PR47：AES-256-GCM，cryptography 库提供 hazmat 层）
cryptography==43.0.3
